const
    Cmd = require('coa/lib/cmd'),
    Opt = require('coa/lib/opt'),
    Arg = require('coa/lib/arg'),
    shell = require('coa/lib/shell');

module.exports = {
    Cmd : Cmd.create,
    Opt : Opt.create,
    Arg : Arg.create,
    classes : { Cmd, Opt, Arg },
    shell,
    require
};
